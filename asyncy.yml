stories:
  app:
    http:
      port: 80

env:
  webhooks:
    jws_token:
    jws_iss: netlify
    jws_verify_iss: true
    jws_algorithm: HS256
    jws_header: X-Webhook-Signature

  s3:
    bucket:
    access_key:
    secret_key:

  github:
    access_token:
    user_agent: Asyncy
